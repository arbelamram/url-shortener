# Dependencies
node_modules/

# Claude Code local config
.claude/

# Environment
server/.env

# Local notes (may contain secrets — never commit)
NOTES.md

# OS artifacts (consider moving desktop.ini to your global ~/.gitignore_global)
desktop.ini
